---
- hosts: all

  pre_tasks:
    - name: Update the apt cache.
      apt: update_cache=yes cache_valid_time=600

# Galaxy Role Servers test.
- import_playbook: ../galaxy-roles-servers/lamp.yml
- import_playbook: ../galaxy-roles-servers/solr.yml
